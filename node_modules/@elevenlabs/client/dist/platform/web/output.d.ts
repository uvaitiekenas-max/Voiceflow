import type { FormatConfig } from "../../utils/BaseConnection.js";
import type { AudioWorkletConfig } from "../../BaseConversation.js";
import type { OutputController, OutputDeviceConfig, OutputConfig, PlaybackEventTarget, PlaybackListener } from "../../OutputController.js";
export declare class MediaDeviceOutput implements OutputController, PlaybackEventTarget {
    private readonly context;
    private readonly analyser;
    private readonly gain;
    private readonly worklet;
    private readonly audioElement;
    static create({ sampleRate, format, outputDeviceId, workletPaths, libsampleratePath, audioContext, }: FormatConfig & OutputConfig & AudioWorkletConfig & {
        audioContext?: AudioContext;
    }): Promise<MediaDeviceOutput>;
    private volume;
    private interrupted;
    private interruptTimeout;
    private readonly volumeProvider;
    private constructor();
    getAnalyser(): AnalyserNode;
    getVolume(): number;
    getByteFrequencyData(buffer: Uint8Array<ArrayBuffer>): void;
    addListener(listener: PlaybackListener): void;
    removeListener(listener: PlaybackListener): void;
    setVolume(volume: number): void;
    playAudio(chunk: ArrayBuffer): void;
    interrupt(resetDuration?: number): void;
    setDevice(config?: Partial<FormatConfig> & OutputDeviceConfig): Promise<void>;
    close(): Promise<void>;
}
//# sourceMappingURL=output.d.ts.map