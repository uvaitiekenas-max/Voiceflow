import type { InputConfig } from "./InputController.js";
import type { OutputConfig, PlaybackEventTarget } from "./OutputController.js";
import type { BaseConnection, FormatConfig } from "./utils/BaseConnection.js";
import type { AgentAudioEvent, InterruptionEvent } from "./utils/events.js";
import { BaseConversation, type Options, type PartialOptions } from "./BaseConversation.js";
import type { InputController } from "./InputController.js";
import type { OutputController } from "./OutputController.js";
export declare class VoiceConversation extends BaseConversation {
    private input;
    private output;
    private playbackEventTarget;
    private cleanUp;
    readonly type = "voice";
    static startSession(options: PartialOptions): Promise<VoiceConversation>;
    private inputFrequencyData?;
    private outputFrequencyData?;
    private handlePlaybackEvent;
    protected constructor(options: Options, connection: BaseConnection, input: InputController, output: OutputController, playbackEventTarget: PlaybackEventTarget | null, cleanUp: () => Promise<void>);
    protected handleEndSession(): Promise<void>;
    protected handleInterruption(event: InterruptionEvent): void;
    protected handleAudio(event: AgentAudioEvent): void;
    private static readonly FREQUENCY_BIN_COUNT;
    setMicMuted(isMuted: boolean): void;
    getInputByteFrequencyData(): Uint8Array<ArrayBuffer>;
    getOutputByteFrequencyData(): Uint8Array<ArrayBuffer>;
    getInputVolume(): number;
    getOutputVolume(): number;
    changeInputDevice({ sampleRate, format, preferHeadphonesForIosDevices, inputDeviceId, }: Partial<FormatConfig> & InputConfig): Promise<void>;
    changeOutputDevice({ sampleRate, format, outputDeviceId, }: Partial<FormatConfig> & OutputConfig): Promise<void>;
    setVolume: ({ volume }: {
        volume: number;
    }) => void;
}
//# sourceMappingURL=VoiceConversation.d.ts.map